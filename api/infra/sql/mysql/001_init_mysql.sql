-- fixed test db
-- CREATE DATABASE IF NOT EXISTS `conductor_test`;
-- GRANT ALL ON conductor_test.* TO user IDENTIFIED BY 'user_pw' WITH GRANT OPTION;

-- USE `conductor_test`;

-- now using fixed named 'test' db (from docker-compose definition)
-- USE `test`;

-- FLUSH PRIVILEGES;

SET time_zone = '+09:00';